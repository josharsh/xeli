# Rust
/target
**/*.rs.bk

# Editor / OS
.DS_Store
.idea/
.vscode/
.claude/
*.swp
*.swo

# Env / secrets
.env
.env.*
!.env.example

# vhs renders — commit GIFs with `git add -f` only when you want them in the repo
tapes/out/
